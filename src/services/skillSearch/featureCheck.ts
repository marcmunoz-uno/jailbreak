/**
 * Runtime feature check for experimental skill search.
 * Returns true when skill search is enabled for the current session.
 */
export function isSkillSearchEnabled(): boolean {
  return false
}

/**
 * Bundled workflows initialization.
 * Registers built-in workflow definitions before the WorkflowTool is used.
 */
export function initBundledWorkflows(): void {
  // no-op stub — bundled workflows will be registered here
}

import React from 'react'

/**
 * WebBrowserPanel — optional UI panel rendered at the bottom of the REPL
 * when the WEB_BROWSER_TOOL feature is active.
 *
 * Currently a no-op stub that renders nothing. A full implementation would
 * display the browser URL bar, navigation controls, and page preview.
 */
export function WebBrowserPanel(): React.ReactElement | null {
  return null
}
